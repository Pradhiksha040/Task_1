<!-- Task 3 Hero Section -->
<section class="hero-section text-center py-5 position-relative overflow-hidden">
  <div class="container py-4 position-relative z-1">
    <div class="badge-pill mb-3">
      <span class="badge bg-primary-subtle text-primary border border-primary-subtle rounded-pill px-3 py-2 fw-semibold">
        <i class="bi bi-stars text-warning me-1"></i> Tasks 2 – 8 Progressive Full Stack Application
      </span>
    </div>

    <h1 class="display-4 fw-extrabold text-white mb-3">
      Enterprise Web Platform with <span class="gradient-text">Real-Time Intelligence</span>
    </h1>

    <p class="lead text-muted mx-auto mb-4" style="max-width: 720px;">
      Explore complex forms, client/server validation, responsive UI layouts, REST APIs, JWT authentication, rate-limiting, external APIs, background jobs, and server-side caching.
    </p>

    <div class="d-flex justify-content-center gap-3 flex-wrap">
      <a href="/register" class="btn btn-primary btn-lg rounded-pill px-4 nav-link-custom shadow-lg" data-link="/register">
        <i class="bi bi-ui-checks me-2"></i> Launch Registration Form
      </a>
      <a href="/users" class="btn btn-outline-light btn-lg rounded-pill px-4 nav-link-custom" data-link="/users">
        <i class="bi bi-database me-2"></i> View REST Users API
      </a>
    </div>

    <!-- Live System Statistics Banner -->
    <div class="row g-3 mt-5 justify-content-center">
      <div class="col-6 col-md-3">
        <div class="glass-stat-card p-3 rounded-4">
          <h3 class="fw-bold mb-0 text-white" id="statSubmissions">0</h3>
          <small class="text-muted">Total Registrations</small>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="glass-stat-card p-3 rounded-4">
          <h3 class="fw-bold mb-0 text-success" id="statStatus">Active</h3>
          <small class="text-muted">Server Status</small>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="glass-stat-card p-3 rounded-4">
          <h3 class="fw-bold mb-0 text-warning" id="statCache">Redis/Memory</h3>
          <small class="text-muted">Cache Engine</small>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="glass-stat-card p-3 rounded-4">
          <h3 class="fw-bold mb-0 text-info" id="statQueue">Ready</h3>
          <small class="text-muted">Background Worker</small>
        </div>
      </div>
    </div>
  </div>
</section>
